block header
block content
    doctype
    html
        head

            body
.col-sm-offset-2.col-sm-4
        h1: label: label-default Sweden Warehouse

        ol(ng-repeat="location in locations"): .sweLocPanel: .whiteText
            h2 {{location._id}}
            p Connected sensor: sensor{{location.sensorID}}
            p Location available: {{location.available}}
            p itemID: {{location.itemID}}
            p Position: {{location.nodeID}}

.col-sm-offset-2.col-sm-4
    h1: label: label-default Vietnam Warehouse

    ol(ng-repeat="location in locations"): .vieLocPanel: .whiteText
        h2 {{location._id}}
        p Connected sensor: sensor{{location.sensorID}}
        p Location available: {{location.available}}
        p itemID: {{location.itemID}}
        p Position: {{location.nodeID}}

