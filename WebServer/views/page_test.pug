block content
  .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
    | testing the database connection:

    li(ng-repeat="sensor in sensors")
        h2 {{sensor._id}}
        p Sunlight: {{sensor.sunlight}}
        | Temperature: {{sensor.temperature}}
